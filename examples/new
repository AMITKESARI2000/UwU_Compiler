stment_seq:
STATEMENT  stment_seq { ircode = $1.nd->code + $2.nd->code; $$.nd=mknode($1.nd, $2.nd,"Statements", ircode ); irtempCount = 0;}| 
STATEMENT {$$.nd = $1.nd; irtempCount = 0; }|
error SEMICOL {std::cout<< "\n\n### Error: "<< "syntax error" <<" in function "<< curr_function <<" in line "<< line_stmt << std::endl; } stment_seq  { ircode = $4.nd->code ; $$.nd=mknode($4.nd, NULL,"Statements", ircode ); irtempCount = 0;}|
NEXTLINE {line_stmt++;} stment_seq   { ircode = $3.nd->code ; $$.nd=mknode($3.nd, NULL,"Statements", ircode ); irtempCount = 0;}
;
