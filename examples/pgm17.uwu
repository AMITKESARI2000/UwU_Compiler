$$ uwu Program for the binary $$
$$ representation of a given number $$

function bin( n)
{
	if (n > 1)
		bin(n / 2);
    let ans = n % 2;
	print ( ans);
}

function main()
{
	bin(7);
	print("\n");
	bin(4);
}