function printFloydTriangle(n)
{
	let i;
    let j;
    let val = 1;
		loop(i<n)
	{
		loop(j<n)
		{
			print ( val + " ");
			val+=1;
    }(j++)
		print("\n");
	}(i = i + 10 - 10 * 10)
}

$$ Driver Code $$
function main()
{
	printFloydTriangle(6);
	return 0;
}

